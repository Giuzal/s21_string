#include "s21_string.h"

size_t s21_strspn(const char *string, const char *charset){
    s21_size_t init_lenght = 0;

    s21_size_t lenght1 = s21_strlen(string);
    s21_size_t lenght2 = s21_strlen(charset);

    for (s21_size_t i = 0; i < lenght1; i++)
    {
        for (s21_size_t j = 0; j < lenght2; j++)
        {
            if (string)
        }
    }
}